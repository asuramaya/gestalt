__pycache__/
*.pyc
.ruff_cache/
dist/
*.tmp
# the bundled venv is built by install.sh, never committed
venv/
.venv/
# local Claude Code / MCP tooling config — machine-specific, not project code
.claude/
.mcp.json
